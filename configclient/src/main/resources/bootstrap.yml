spring:
  application:
    name: configclient
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: production



management:
  security:
    enabled: false

#  instance:
#    statusPageUrlPath: /my_info
#    healthCheckUrlPath: /my_health

